<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="de.juehvtech.verein"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk
            android:minSdkVersion="10"
            android:targetSdkVersion="18"/>

    <permission android:name="android.permission.READ_CONTACTS">
    </permission>

    <application
            android:allowBackup="false"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name">
        <activity
                android:name=".EventSheetActivity"
                android:label="@string/activity_attandence_sheet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".EventContentActivity"></activity>
        <activity
                android:name=".administration.MenuActivity"
                android:label="@string/activity_admin"
                android:screenOrientation="portrait">
        </activity>

    </application>

</manifest>